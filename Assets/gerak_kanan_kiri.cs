using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class gerak_kanan_kiri : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //membuat gerak kanan kiri
    
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
